streamlit
streamlit-float
gspread
google-auth
google-auth-httplib2
google-auth-oauthlib
# ⚠️ 네이티브 데이터 스택 버전 고정
# 미고정 상태에서 Streamlit Cloud가 Python 3.14 + pandas 3.0 + numpy 2.5 + pyarrow 25
# (전부 최신)를 설치하면서, BigQuery 결과 → DataFrame 변환 중 Segmentation fault(앱 크래시).
# 문서상 목표 환경은 Python 3.11. 3.13 이하에선 안정버전으로 고정, 3.14에선 설치 실패를
# 막기 위해 wheel 있는 최신으로 폴백(단, 3.14는 크래시 우려 → 앱 설정에서 Python 3.12 권장).
pandas==2.2.3; python_version < "3.14"
pandas; python_version >= "3.14"
numpy==2.1.3; python_version < "3.14"
numpy; python_version >= "3.14"
pyarrow==18.1.0; python_version < "3.14"
plotly
anthropic
google-cloud-bigquery
db-dtypes
requests
beautifulsoup4
